name: Cut over CRM to crmsatori.ru

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cutover-crm-ru.yml'
  workflow_dispatch:

jobs:
  cutover:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check DNS readiness
        id: dns
        run: |
          set -eu
          echo '=== crmsatori.ru DNS ==='
          auth=$(dig +short crmsatori.ru A | tail -1)
          ns=$(dig +short NS crmsatori.ru | tr '\n' ' ')
          echo "nameservers=$ns"
          echo "resolved=$auth"
          if [ "$auth" != "194.226.97.103" ]; then
            echo "crmsatori.ru currently resolves to '$auth', expected 194.226.97.103."
            echo 'Point the root A record (@) of crmsatori.ru to 194.226.97.103 before cutover.'
            exit 1
          fi

          ready=false
          for i in $(seq 1 30); do
            cf=$(dig +short @1.1.1.1 crmsatori.ru A | tail -1)
            gg=$(dig +short @8.8.8.8 crmsatori.ru A | tail -1)
            echo "attempt=$i cloudflare=$cf google=$gg"
            if [ "$cf" = "194.226.97.103" ] && [ "$gg" = "194.226.97.103" ]; then
              ready=true
              break
            fi
            sleep 10
          done
          if [ "$ready" != "true" ]; then
            echo 'crmsatori.ru has not propagated to public resolvers yet.'
            exit 1
          fi
          echo 'ready=true' >> "$GITHUB_OUTPUT"

      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: Trust production server
        run: ssh-keyscan -H 194.226.97.103 >> ~/.ssh/known_hosts

      - name: Configure crmsatori.ru and certificate
        run: |
          ssh root@194.226.97.103 'bash -se' <<'REMOTE'
          set -eu
          old=/etc/nginx/conf.d/satori-crm.conf
          new=/etc/nginx/conf.d/crmsatori-ru.conf
          tmp=/etc/nginx/conf.d/crmsatori-ru-acme.conf
          stamp=$(date +%Y%m%d-%H%M%S)
          mkdir -p /var/backups/satori-crm-hostname-$stamp
          cp -a "$old" /var/backups/satori-crm-hostname-$stamp/satori-crm.conf

          cat >"$tmp" <<'NGINX'
          server {
              listen 192.168.0.38:80;
              server_name crmsatori.ru;
              location ^~ /.well-known/acme-challenge/ {
                  root /var/www/letsencrypt;
                  default_type text/plain;
                  try_files $uri =404;
              }
              location / { return 301 https://$host$request_uri; }
          }
          NGINX

          nginx -t
          systemctl reload nginx
          mkdir -p /var/www/letsencrypt

          if [ ! -f /etc/letsencrypt/live/crmsatori.ru/fullchain.pem ]; then
            certbot certonly --webroot -w /var/www/letsencrypt \
              -d crmsatori.ru \
              --non-interactive --agree-tos --register-unsafely-without-email
          fi

          python3 - <<'PY'
          from pathlib import Path
          src=Path('/etc/nginx/conf.d/satori-crm.conf').read_text()
          src=src.replace('crm.satorilabural.online','crmsatori.ru')
          src=src.replace('/etc/letsencrypt/live/crm.satorilabural.online/','/etc/letsencrypt/live/crmsatori.ru/')
          Path('/etc/nginx/conf.d/crmsatori-ru.conf').write_text(src)
          PY

          rm -f "$tmp"
          nginx -t
          systemctl reload nginx

          curl --fail --silent --show-error --resolve crmsatori.ru:443:192.168.0.38 \
            https://crmsatori.ru/login >/dev/null
          curl --fail --silent --show-error http://127.0.0.1:3030/health >/dev/null
          curl --fail --silent --show-error http://127.0.0.1:3020/api/deals >/dev/null
          echo 'crmsatori.ru is configured and healthy.'
          REMOTE

      - name: Verify crmsatori.ru externally
        run: |
          curl -4 --fail --silent --show-error --connect-timeout 10 --max-time 20 \
            https://crmsatori.ru/login >/dev/null
          echo 'crmsatori.ru is externally reachable.'
